#pragma once

enum VFS_FTYPE
{
	VFS_FILE,
	VFS_DIR
};

enum VFS_OPERATION
{
	VFS_MOUNT,
	VFS_UNMOUNT,

	VFS_OPEN,
	VFS_CLOSE,

	VFS_READ,
	VFS_WRITE
};

enum VFS_REQUEST
{
	VFS_REQ_FIND,

	VFS_REQ_READ,
	VFS_REQ_WRITE
};
